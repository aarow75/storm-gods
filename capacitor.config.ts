import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'RuneQuest',
  webDir: 'dist/runequest-characters/browser'
};

export default config;
